import { Injectable, signal } from '@angular/core';
import { Juegos } from '../models/juegos';
@Injectable({
  providedIn: 'root',
})
export class Carrito {
  // carrito=signal<Juegos[]>([]);
  // agregar(p:juegos){
  //   this.
  // }
}
